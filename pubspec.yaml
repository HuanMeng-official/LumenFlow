name: lumen_flow
description: "Chat With Your AI"
publish_to: "none"
version: 2.1.0+210

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.5.0
  shared_preferences: ^2.5.3
  intl: any
  flutter_markdown_plus: ^1.0.7
  image_picker: ^1.2.0
  file_picker: ^10.3.7
  path_provider: ^2.1.5
  path: ^1.9.1
  flutter_svg: ^2.0.10
  pdf: ^3.11.3
  archive: ^4.0.7
  url_launcher: ^6.3.1
  flutter_local_notifications: ^19.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  generate: true
  assets:
    - assets/prompt/presets-en.json
    - assets/prompt/presets-zh.json
    - assets/prompt/presets-ja.json
    - assets/prompt/presets-ko.json
    - assets/prompt/characters/
    - assets/prompt/characters/zh/
    - assets/prompt/characters/en/
    - assets/prompt/characters/ja/
    - assets/prompt/characters/ko/
    - assets/logo.png
    - assets/collection_code.png
    - assets/platform/
    - pubspec.yaml

